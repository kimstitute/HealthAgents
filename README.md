# HealthAgents

🏗️ 사용자 중심의 AI 코칭 에이전트 아키텍처
이 아키텍처는 사용자가 보는 클라이언트(Client) 측과 이를 지원하는 서버(Server) 측의 역할 분담을 명확히 보여줍니다.

1. 📱 클라이언트 레이어 (사용자 상호작용 및 입력)
이 레이어는 사용자가 직접 만지는 모든 요소이며, 아키텍처의 **가장 앞단(Front)**에 위치합니다.

Front-end App (Mobile/Web):

역할: 사용자 인터페이스(UI) 제공. 사용자 프로필 설정, 식단/운동 기록 입력, 코칭 피드백 확인 등 모든 대화 및 데이터 입력의 주요 창구입니다.

기술: Firebase Hosting을 이용한 웹 또는 Android/iOS 앱.

갤럭시 워치 / 삼성 헬스:

역할: 활동량, 수면, 심박수 등 객관적인 센서 데이터를 수집하여 Front-end 앱이나 Firebase로 전송합니다.

Notion (부가 입력):

역할: Front-end 앱을 사용하지 않고 구조화된 데이터(예: 주간 일지, 목표)를 입력하는 보조 입력 채널 역할을 합니다.

Google Calendar:

역할: AI가 생성한 최종 일정을 확인하는 출력/확인 채널 역할을 합니다.

2. ⚙️ 백엔드 및 프로세싱 레이어 (데이터 처리 및 코칭 로직)
클라이언트의 요청을 받아 모든 연산을 처리하고 지능형 서비스를 연결하는 **중앙 처리 장치(Middle)**입니다.

Firebase Cloud Functions (중앙 허브):

역할: 클라이언트 앱의 API 요청을 수신하거나, Notion/삼성 헬스의 **이벤트(Webhook)**를 수신하여 모든 로직을 실행합니다.

Firebase Firestore / Realtime DB:

역할: 사용자 프로필, 코칭 기록, 워치 원천 데이터 등 모든 정보를 안전하게 저장하고 관리합니다.

3. 🧠 지능형 서비스 및 외부 연동 레이어 (AI & Action)
백엔드 뒤에서 지능을 제공하고, 실제 외부 시스템에 명령을 내리는 **지원 시스템(Back)**입니다.

OpenAI API (GPT 모델):

역할: DB의 사용자 데이터(주관/객관)를 분석하고, 최적화된 맞춤형 코칭 내용 및 구조화된 일정 데이터를 생성하여 Firebase에 반환합니다.

외부 API 커넥터:

Google Calendar API: AI가 생성한 운동/식단 일정을 사용자의 캘린더에 푸시.

Notion API: AI가 생성한 주간 분석 보고서 및 피드백을 Notion DB에 업데이트.

Samsung Health / Android API: 운동 기록을 받거나 알림을 전달합니다.
